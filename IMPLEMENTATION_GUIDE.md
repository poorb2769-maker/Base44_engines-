# Base44 Engine - Implementation Guide\n\n## Overview\nComprehensive implementation guide for all 7 phases of the Base44 game engine.\n\n---\n\n## Phase 1: Core Systems ✅ COMPLETED\n\n### Physics System\n```typescript\nimport { Rigidbody, Collider, ColliderShape } from './engine';\n\n// Create rigidbody\nconst rb = new Rigidbody({\n  mass: 1,\n  friction: 0.5,\n  restitution: 0.3,\n  useGravity: true\n});\n\n// Create collider\nconst collider = new Collider({\n  shape: ColliderShape.Box,\n  size: new Vector3(1, 1, 1),\n  isTrigger: false\n});\n\n// Apply forces\nrb.applyForce(new Vector3(0, 10, 0));\nrb.applyImpulse(new Vector3(5, 0, 0));\n```\n\n### Input System\n```typescript\nimport { InputManager, KeyCode, MouseButton } from './engine';\n\nconst input = new InputManager(canvas);\n\n// Keyboard input\nif (input.isKeyPressed(KeyCode.W)) {\n  // Move forward\n}\n\n// Mouse input\nconst mousePos = input.getMousePosition();\nconst leftClicked = input.isMouseButtonPressed(MouseButton.Left);\n\n// Action bindings\ninput.bindAction('jump', [KeyCode.Space]);\ninput.bindAction('interact', [KeyCode.E]);\n\nif (input.isActionPressed('jump')) {\n  character.jump();\n}\n\n// Gamepad support\ninput.updateGamepads();\nconst gamepadState = input.getGamepadState(0);\nif (gamepadState?.buttons[0]) {\n  // A button pressed\n}\n```\n\n### Animation System\n```typescript\nimport { AnimationController } from './engine';\n\nconst animController = new AnimationController();\nanimController.initializeMixer(model);\n\n// Play animation\nanimController.play('walk', true);\n\n// Transition between animations\nanimController.transition('run', 0.5); // 0.5s transition\n\n// Update in render loop\nfunction render() {\n  animController.update(deltaTime);\n}\n```\n\n### Audio System\n```typescript\nimport { AudioManager } from './engine';\n\nconst audio = new AudioManager();\n\n// Load audio\nawait audio.loadAudio('bgm', 'path/to/music.wav');\nawait audio.loadAudio('sfx', 'path/to/sound.wav');\n\n// Play audio\naudio.play('bgm', { volume: 0.5, loop: true });\naudio.play('sfx', { volume: 0.8, fade: 0.5 });\n\n// Control volume\naudio.setMasterVolume(0.7);\n\n// Resume audio context (required by browsers)\naudio.resumeAudioContext();\n```\n\n---\n\n## Phase 2: Rendering & Effects ✅ COMPLETED\n\n### Shadow System\n```typescript\nimport { ShadowSystem } from './engine';\n\nconst shadows = new ShadowSystem(renderer, {\n  type: 'cascaded',\n  resolution: 2048\n});\n\n// Add directional light (sun)\nconst sunLight = shadows.addDirectionalLight(\n  'sun',\n  { x: 10, y: 20, z: 10 },\n  1.0,\n  true\n);\n\n// Add point light\nconst lampLight = shadows.addPointLight(\n  'lamp',\n  { x: 0, y: 2, z: 0 },\n  0.8,\n  50\n);\n\n// Update light position\nshadows.updateLightPosition('sun', { x: 15, y: 25, z: 15 });\n```\n\n### Post-Processing Effects\n```typescript\nimport { PostProcessing } from './engine';\n\nconst postFX = new PostProcessing(renderer, scene, camera);\n\n// Add bloom (anime glow)\npostFX.addBloomEffect(0.5, 0.85);\n\n// Add cinematic effects\npostFX.addDepthOfFieldEffect(5, 0.5);\npostFX.addFilmGrainEffect(0.35);\npostFX.addColorGrading(1.2, 1.1);\n\n// Control effects\npostFX.setEffectEnabled('bloom', true);\npostFX.setEffectParameter('dof', 'focusDistance', 10);\n\n// Apply in render loop\nfunction render() {\n  postFX.applyEffects();\n}\n```\n\n### Particle System\n```typescript\nimport { ParticleSystem, EmitterConfig } from './engine';\n\nconst particles = new ParticleSystem();\n\n// Create explosion effect\nconst explosionConfig: EmitterConfig = {\n  position: new Vector3(0, 0, 0),\n  emission: 100, // particles per second\n  lifetime: 2.0,\n  speed: 10,\n  speedVariation: 5,\n  size: 0.5,\n  sizeVariation: 0.2,\n  color: new Color(0xff4400),\n  particleCount: 1000\n};\n\nconst emitter = particles.createEmitter('explosion', explosionConfig);\n\n// Update particles\nfunction render() {\n  particles.update(deltaTime);\n  // Add particle meshes to scene\n  particles.getMeshes().forEach(mesh => scene.add(mesh));\n}\n```\n\n---\n\n## Phase 3: Editor Tools ✅ COMPLETED\n\n### Gizmo System\n```typescript\nimport { GizmoSystem, GizmoMode, TransformSpace } from './engine';\n\nconst gizmos = new GizmoSystem(camera, canvas);\n\n// Select entity\ngizmos.selectEntity(myEntity);\n\n// Set transform mode\ngizmos.setMode(GizmoMode.Translate); // W key\ngizmos.setMode(GizmoMode.Rotate);    // E key\ngizmos.setMode(GizmoMode.Scale);     // R key\n\n// Toggle transform space\ngizmos.setSpace(TransformSpace.World);\ngizmos.setSpace(TransformSpace.Local);\n```\n\n### Prefab System\n```typescript\nimport { PrefabSystem } from './engine';\n\nconst prefabs = new PrefabSystem();\n\n// Save entity as prefab\nconst prefabData = prefabs.createPrefab('Player', playerEntity);\n\n// Instantiate prefab\nconst instance = prefabs.instantiatePrefab(prefabData.id);\n\n// Override prefab values\nprefabs.setInstanceOverride(instance.id, 'health', 150);\n\n// Update prefab (updates all instances)\nprefabs.updatePrefab(prefabData.id, { name: 'PlayerV2' });\n\n// Get prefab library\nconst allPrefabs = prefabs.getAllPrefabs();\n```\n\n### Hierarchy System\n```typescript\nimport { HierarchySystem } from './engine';\n\nconst hierarchy = new HierarchySystem();\n\n// Create node\nconst player = hierarchy.createNode('Player', 'root');\nconst weapon = hierarchy.createNode('Weapon', player.id);\n\n// Move node\nhierarchy.moveNode(weapon.id, 'root');\n\n// Rename\nhierarchy.renameNode(player.id, 'PlayerCharacter');\n\n// Get children\nconst playerChildren = hierarchy.getChildren(player.id);\n\n// Toggle active/lock\nhierarchy.toggleActive(player.id);\nhierarchy.toggleLock(weapon.id);\n```\n\n---\n\n## Phase 4: Gameplay Systems ✅ COMPLETED\n\n### Character Controller\n```typescript\nimport { CharacterController, CharacterState } from './engine';\n\nconst character = new CharacterController(rigidbody);\n\n// Handle input\nif (input.isKeyPressed(KeyCode.W)) {\n  character.setMoveInput(0, 1);\n}\nif (input.isKeyPressed(KeyCode.Space)) {\n  character.jump();\n}\n\ncharacter.setSprint(input.isKeyPressed(KeyCode.Shift));\n\n// Update in game loop\ncharacter.update(deltaTime);\n\n// Get state for animation\nconst state = character.getState();\nswitch (state) {\n  case CharacterState.Idle:\n    animController.play('idle');\n    break;\n  case CharacterState.Walk:\n    animController.play('walk');\n    break;\n  case CharacterState.Run:\n    animController.play('run');\n    break;\n}\n```\n\n### Combat System\n```typescript\nimport { CombatSystem, AttackConfig } from './engine';\n\nconst combat = new CombatSystem(100); // 100 health\n\n// Register attacks\nconst punchAttack: AttackConfig = {\n  name: 'punch',\n  damage: 10,\n  knockback: 5,\n  stunDuration: 0.2,\n  cooldown: 0.5\n};\ncombat.registerAttack(punchAttack);\n\n// Perform attack\nif (input.isMouseButtonPressed(MouseButton.Left)) {\n  if (combat.performAttack('punch')) {\n    animController.play('attack_punch');\n  }\n}\n\n// Take damage\ncombat.takeDamage({\n  amount: 10,\n  source: 'punch',\n  type: 'physical',\n  knockback: new Vector3(0, 0, -5)\n});\n\n// Update combat state\ncombat.update(deltaTime);\n\n// Check status\nif (!combat.isAlive()) {\n  // Game over\n}\n\nconst healthPercent = combat.getHealthPercent();\nconst comboCount = combat.getComboCount();\n```\n\n### Dialogue System\n```typescript\nimport { DialogueSystem, DialogueTree, DialogueNode } from './engine';\n\nconst dialogue = new DialogueSystem();\n\n// Create dialogue tree\nconst npcDialogue: DialogueTree = {\n  id: 'npc_greeting',\n  title: 'NPC Greeting',\n  startNodeId: 'greeting',\n  nodes: new Map([\n    [\n      'greeting',\n      {\n        id: 'greeting',\n        character: 'NPC',\n        text: 'Hello, adventurer!',\n        choices: [\n          {\n            text: 'Hello!',\n            nextNodeId: 'response1',\n            action: () => console.log('Greeted NPC')\n          },\n          {\n            text: 'Do you have quests?',\n            nextNodeId: 'quests'\n          }\n        ]\n      }\n    ],\n    [\n      'response1',\n      {\n        id: 'response1',\n        character: 'NPC',\n        text: 'Nice to meet you!',\n        choices: [{ text: 'Goodbye', nextNodeId: null }]\n      }\n    ]\n  ])\n};\n\ndialogue.registerTree(npcDialogue);\n\n// Start dialogue\ndialogue.startDialogue('npc_greeting');\n\n// Display current node\nconst currentNode = dialogue.getCurrentNode();\nconsole.log(`${currentNode.character}: ${currentNode.text}`);\n\n// Display choices\nconst choices = dialogue.getAvailableChoices();\nchoices.forEach((choice, i) => console.log(`${i}: ${choice.text}`));\n\n// Make choice\ndialogue.makeChoice(0);\n\n// Listen to events\ndialogue.on('dialogueEnd', () => console.log('Dialogue finished'));\n```\n\n### AI System\n```typescript\nimport { AISystem, AIState } from './engine';\n\nconst enemy = new AISystem({\n  state: AIState.Patrol,\n  attackCooldown: 1.0,\n  detectionRange: 20,\n  attackRange: 2\n});\n\n// Set patrol path\nenemy.setPatrolPath([\n  new Vector3(0, 0, 0),\n  new Vector3(10, 0, 0),\n  new Vector3(10, 0, 10),\n  new Vector3(0, 0, 10)\n]);\n\n// Update AI\nfunction gameLoop() {\n  const velocity = enemy.update(deltaTime, enemyPosition);\n  enemyPosition.add(velocity.multiplyScalar(deltaTime));\n  \n  const state = enemy.getState();\n  // Update animations based on state\n}\n\n// Player enters detection range\nenemy.setTargetEntity(player);\n// AI switches to Chase state\n```\n\n### Cinematic System\n```typescript\nimport { CinematicSystem, CinematicSequence } from './engine';\n\nconst cinematics = new CinematicSystem(camera);\n\n// Create cutscene sequence\nconst openingCutscene: CinematicSequence = {\n  id: 'opening',\n  name: 'Opening Scene',\n  duration: 10,\n  keyFrames: [\n    {\n      time: 0,\n      position: new Vector3(0, 5, -20),\n      fov: 45\n    },\n    {\n      time: 5,\n      position: new Vector3(10, 3, 0),\n      fov: 35\n    },\n    {\n      time: 10,\n      position: new Vector3(0, 2, 10),\n      fov: 60\n    }\n  ]\n};\n\ncinematicsregisterSequence(openingCutscene);\n\n// Play sequence\ncinema.playSequence('opening');\n\n// Update in render loop\ncinema.update(deltaTime);\n\n// Listen to events\ncinema.on('sequenceEnd', () => console.log('Cutscene finished'));\n```\n\n---\n\n## Phase 5: Optimization ✅ COMPLETED\n\n### Culling System\n```typescript\nimport { CullingSystem } from './engine';\n\nconst culling = new CullingSystem(camera);\n\n// Update culling each frame\nfunction render() {\n  culling.update(allObjects, deltaTime);\n  \n  // Only render visible objects\n  const visible = culling.getVisibleObjects();\n  // Renderer automatically uses visible property\n}\n```\n\n### LOD System\n```typescript\nimport { LODSystem } from './engine';\n\nconst lod = new LODSystem();\n\n// Register LOD group for distant object\nlod.registerLODGroup('tree', [\n  { distance: 0, mesh: highQualityTree },\n  { distance: 50, mesh: mediumQualityTree },\n  { distance: 100, mesh: lowQualityTree }\n]);\n\n// Update LOD\nfunction render() {\n  lod.setCameraPosition(camera.position);\n  const correctMesh = lod.updateLOD('tree', treePosition);\n  // Use correctMesh in scene\n}\n```\n\n### Performance Profiler\n```typescript\nimport { Profiler } from './engine';\n\nconst profiler = new Profiler();\n\n// Record each frame\nfunction render() {\n  // ... render code ...\n  profiler.recordFrame();\n  \n  // Display stats\n  const stats = profiler.getStats();\n  console.log(`FPS: ${stats.fps}, Memory: ${stats.memory}MB`);\n}\n```\n\n---\n\n## Phase 6 & 7: Documentation & Samples\n\n### Getting Started\n1. Install dependencies: `npm install`\n2. Start development: `npm run dev`\n3. Build project: `npm run build`\n4. Generate docs: `npm run docs`\n\n### Creating Your First Game\n\n1. **Setup Scene**\n```typescript\nconst scene = new THREE.Scene();\nconst camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight);\nconst renderer = new THREE.WebGLRenderer();\n```\n\n2. **Add Character**\n```typescript\nconst character = new CharacterController(rigidbody);\n```\n\n3. **Add Combat**\n```typescript\nconst combat = new CombatSystem(100);\n```\n\n4. **Game Loop**\n```typescript\nfunction gameLoop() {\n  requestAnimationFrame(gameLoop);\n  \n  // Update systems\n  character.update(deltaTime);\n  combat.update(deltaTime);\n  animController.update(deltaTime);\n  \n  // Render\n  renderer.render(scene, camera);\n}\n```\n\n---\n\n## Best Practices\n\n### Performance\n- Use culling for large scenes\n- Implement LOD for distant objects\n- Monitor FPS with profiler\n- Batch draw calls when possible\n\n### Code Organization\n- Keep systems modular\n- Use prefabs for repeated entities\n- Organize with hierarchy\n- Use undo/redo for editor changes\n\n### Gameplay\n- Design with mobile in mind\n- Test on target devices\n- Balance difficulty\n- Provide clear feedback (audio, visual)\n\n---\n\n## Troubleshooting\n\n### Low FPS\n1. Check profiler stats\n2. Enable culling\n3. Reduce draw calls\n4. Lower shadow resolution\n\n### Audio Issues\n1. Call `resumeAudioContext()`\n2. Check browser autoplay policies\n3. Verify audio files are loaded\n\n### Physics Problems\n1. Check rigidbody mass values\n2. Verify colliders are set correctly\n3. Ensure gravity is enabled\n\n---\n\n## Next Steps\n\n1. Review sample games in `/examples/`\n2. Read API documentation in `/docs/`\n3. Watch tutorial videos\n4. Join community Discord\n5. Start building your game!\n\nHappy game development! 🚀\n